$("#results").html(" <%= escape_javascript (render partial: 'results', :locals =>{:tabs =>@tabs}) %>");
$('#paginator').html(" <%= escape_javascript (paginate(@tabs_keys_paginatable, :remote => true).to_s) %>");
$('.chord').chordsIO();
