<%= form_for(@user_song) do |f| %>
  <% if @user_song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_song.errors.count, "error") %> prohibited this user_song from being saved:</h2>

      <ul>
      <% @user_song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :difficulty %><br>
    <%= f.number_field :difficulty %>
  </div>
  <div class="field">
    <%= f.label :saved %><br>
    <%= f.check_box :saved %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :song_id %><br>
    <%= f.number_field :song_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
